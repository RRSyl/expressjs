const config = {
    development:{
        username: "postgres",
        password: "postgres",
        database: "Expressjs",
        host: "localhost",
        dialect: "postgres"
    }
}
module.exports = config;